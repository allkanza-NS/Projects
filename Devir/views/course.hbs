
    <div class="col s6 offset-s3">
        <div class="course">

            <h3>{{course.title}}</h3>

            <p class="text">{{course.text}}</p>

            <img src="{{course.img}}" alt="{{course.title}}">
            <br><br>
        </div>
    </div>
